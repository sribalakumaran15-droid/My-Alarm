# Daylight Alarm App

A browser-first alarm planner with local storage, responsive layout, and no backend dependency.

Open `index.html` directly, or run it with Vite using `npm install` followed by `npm run dev`.
